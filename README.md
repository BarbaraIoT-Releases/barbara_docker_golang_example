Basic docker container that builds a Golang app and access Barbara OS app config to show different values.

App config json sample:

```
{
    "config1": true,
    "config2": "someValue2"
}
```